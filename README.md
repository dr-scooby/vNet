Java Swing app using the JInternalFrame and JDesktop. The JInternalFrame allows for multiple windows, multiple frames within one application called the JDesktop.

A ping utitlity with DNS lookup.
SMB file explorer. Connect to Windows box or Linux box and get a file listing, like a file explorer.

Using Java threads.
